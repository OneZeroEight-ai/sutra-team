/**
 * Layout for law.sutra.team routes.
 * No sutra.team Header/Footer — law has its own minimal chrome.
 */

export default function LawLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
